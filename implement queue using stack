class Solution:

    def __init__(self):
        self.s1=[]
        self.s2=[]
    
    def push(self,x):
        self.s1.append(x)
    
    def pop(self):
        if len(self.s2)==0:
            while s1:
                self.s2.append(self.s1.pop())
        return self.s2.pop()
    
    def peek(self):
        if len(self.s2)==0:
            while s1:
                self.s2.append(self.s1.pop())
        return self.s2[0]
    
    def isEmpty(self):
        return len(self.s1)==0 and len(self.s2)==0
    
    def printstack(self):
        for i in range(len(self.s1)):
            print(self.s1[i])


obj = Solution()

obj.push(10)
obj.push(20)
obj.push(30)

obj.printstack()
