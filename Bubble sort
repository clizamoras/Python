class Solution:
 def bubble(self,arr):
  n=len(arr)
  for i in range(n):
   for j in range(n-1-i):
     if arr[j]>arr[j+1]:
      arr[j],arr[j+1]=arr[j+1],arr[j]
  return arr
arr=[5,3,8,1,2]
obj=Solution()
print(obj.bubble(arr))
