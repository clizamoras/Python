
class Solution:
  def vector(self,arr):
    result=[]
    n=len(arr)
    count={}
    for i in range(n):
      if arr[i] in count:
         count[arr[i]]+=1
     if arr[i] not in count:
          count[arr[i]]=1
     if count[arr[i]] ==3:
          result.append(arr[i])
   retutn result
arr=[33,11,33,11,11,33]
obj=Solution()
print(obj.vector(arr))
          
    
