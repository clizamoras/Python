
class Solution:
  def selection(self,arr):
    n=len(arr)
    for i in range(n):
      index=i
      for j in range(i+1,n):
        if arr[j]<arr[index]:
          index=j
      arr[j],arr[index]=arr[index],arr[j]
  return arr
arr=[5,3,8,1,2]
obj=Solution()
print(obj.selection(arr))
