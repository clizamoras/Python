

class Solution:
  def sums(self,arr,target):
    n=len(arr)
    count=0
    for i in range(n):
      sum=0
      for j in range(i,n):
         sum+=arr[j]
         if(sum==target):
            count+=1
arr=[3,1,2,4]
obj=Solution()
print(obj.sums(arr,6))
