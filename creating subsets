class Solution:
    def subset(self,a):
        n=len(a)
        for m in range(2**n):
            l=[]
            for i in range(n):
                if m &(1<<i):
                  l.append(a[i])
            print(l)
obj=Solution()
a=[1,2,3]
obj.subset(a)
