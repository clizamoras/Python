class Solution:
    def divisor(self,num):
        l=[]
        for i in range(1,num+1):
            if num%i==0:
                l.append(i)
        return l
obj=Solution()
num=int(input("Enter the number"))
print(obj.divisor(num))
